# Copier en .env et remplir. NE JAMAIS committer le vrai .env (déjà ignoré).
# Générer KEY/SECRET : openssl rand -hex 32

# --- Base de données (dev local) ---
DB_DATABASE=mb74_cms
DB_USER=mb74
DB_PASSWORD=change-me
DB_ROOT_PASSWORD=change-me-root

# --- Directus ---
DIRECTUS_KEY=remplacer-par-openssl-rand-hex-32
DIRECTUS_SECRET=remplacer-par-openssl-rand-hex-32
PUBLIC_URL=http://localhost:8055

# --- Compte admin initial ---
ADMIN_EMAIL=admin@motorboat74.com
ADMIN_PASSWORD=change-me-strong
